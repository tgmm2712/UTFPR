#include<stdio.h>
#include<stdlib.h>
int main(){
// o ponto(.) acessa  o campo na struct
//leitura ($a.coef);
//leitura de string a.nome
// exprecao (a.coef*)+ (a.coef/10)


struct pessoa_s
{
    char nome[64];
    int nasc_d,nasc_m,nasc_a;
    long long cpf;
    int cad_d, cad_m,cad_a;
};
//declaracao de registro
struct aluno a;

struct pessoas_s
{
    char nome[64];
    struct data_S nasc;
    long long cpf;
    struct data_s cad

    
};



typedef struct aluno_t estudandte;


//chamar= estudande a=("tiago", 19999, 0.192);


struct pessoas_s
{
    char nome[64];
    struct data_S nasc;
    long long cpf;
    struct data_s cad

    
}aluno_t;


scanf("%d",&aluno.nome);
scanf("%d",&[i].nasc.rua);
/*prova possivelconter  p-> equivale (*p);*/

scanf("%d",&pa->ra);
//mesma coisa(ponteiro para struck)
scanf("%d",&(*pa).ra);


















}
